MVC -> M - Model, V - View, C - Controller

React is a javascript library

Features
1) Component based -> build the UI from small, reusable pieces
2) Virtual DOM -> fast updates by changing what actually differs
3) One-way data flow -> data flow (parent -> child) via props
4) SPA -> Single page application